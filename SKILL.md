---
name: equity-research-harness
description: 株式投資目線での個別企業の深掘り調査を、証拠台帳とWチェックで裏取りしながら実行するハーネス。対象企業が決まっている前提で使う（候補を広く探すスクリーニング用途ではない）。企業分析、銘柄調査、業界構造の把握、決算の読み解き、投資仮説の検証、競合比較、マクロ・セクター動向の整理を求められたら必ずこのSkillを使う。「◯◯を調べて」「この会社どう思う」「決算を見て」「業界地図を作って」「仮説を検証したい」といった依頼、また出典付き・ハルシネーション厳禁が求められる調査全般が対象。日本株・米国株どちらも対象で、レポート出力だけでなく調査の途中経過チェックにも使う。
---

# 企業調査ハーネス（Equity Research Harness）

## 適用範囲

**個別企業の深掘り調査に特化する。** 対象はユーザが指定する。
テーマから候補を広く探して絞り込むスクリーニング用途は範囲外
（膨大な調査量が必要で、証拠台帳を伴う本ハーネスの粒度に合わないため）。
業界・マクロの調査は、対象企業を理解するための文脈として行う。

## このSkillが解いている問題

投資調査でのAIの失敗は「知らないことを書く」ではなく、**もっともらしい数字と因果を埋めてしまうこと**で起きる。
本ハーネスは、生成の自由度を下げるのではなく、**書いた内容を機械的に反証できる状態にする**ことで防ぐ。

中核ルール（これだけは崩さない）:

1. **原文がローカルに保存されていない情報は、レポートに書けない。** 記憶・要約・検索スニペットからの記述は禁止。
   **台帳に原文を転記せず、識別語（anchor_head/tail）と位置で指す。** 引用文はスクリプトが原文から読み出す（`references/evidence.md`）。
   PDFは全文、Webページは本文全体を保存する（一部だけ切り出さない）。読解範囲は `references/evidence.md`。
2. **すべての事実記述は証拠ID（E-xxxx）を持つ。** 証拠IDは原文の完全一致部分文字列に紐づく。
3. **検証はスクリプトが行い、Claudeは判定しない。** Claudeの「確認しました」は証拠にならない。
   ゲートは実行証跡（`checks/receipt_*.json`）を残し、`verify_gate.py` で第三者が確認できる。
4. **知らないことは5つのステータスで書き分ける。** 空欄・沈黙・ぼかしは禁止。

---

## ワークフロー（フェーズとゲート）

調査は必ず区切る。ゲートを通らないまま次に進まない。ゲート未通過のまま出力すると、
誤りが下流に伝播して検出コストが跳ね上がるため、**ここは省略しない**。

| Phase | 内容 | 出口ゲート |
|---|---|---|
| P0 | スコープ・投資仮説・必要証拠リストの確定 | **G0**: ユーザ合意（対象/期間/深度/使途）＋採用する問いの選択 |
| P1 | 一次情報の特定と取得、原文をローカル保存 | **G1**: ソース棚卸し＋不足分のユーザ依頼発行 |
| P2 | 証拠台帳（ledger）化 | **GA**: `check_a` PASS（ソース⇔台帳） |
| P3 | 分析・図解・仮説検証 | **GF**: 図表データが全てE-ID参照 |
| P4 | レポート起草 | **GB**: `check_b` + `lint_status` PASS（台帳⇔レポート） |
| P4.5 | 独立レビュー（主張⇔引用の意味照合。サブエージェント可） | **GC**: `check_c_claims_review.py` PASS |
| P5 | 納品・対話 | **G5**: 会話ログから改善候補抽出→汎用性ゲート |

**P0では、仮説を立てた後に `references/questions.md`（問いの棚）を開き、今回使う問いをIDで選ぶ。**
先に開くとリストに引きずられて仮説が凡庸になる。選ばなかった問いはレポートに列挙せず、
スコープ宣言で一括処理する。

**分割の基準**: 1ゲートあたり ソース15本 または 証拠80件 を超えたら、調査対象を分割して
ゲートを追加する（例: 事業構造→財務→競争環境 で3周回す）。長い一括処理は取りこぼしを生む。

詳細手順は `references/workflow.md` を読む。

---

## 作業ディレクトリ構成

調査案件ごとに1ディレクトリを作る（`research/<対象>_<YYYYMMDD>/`）。

```
research/<case>/
  sources.json          # ソース一覧（メタデータ、ハッシュ、取得手段）
  raw/                  # 原文そのまま（PDF/HTML原本）
  raw_text/             # 抽出済みプレーンテキスト S-001.txt ...
  ledger.jsonl          # 証拠台帳（1行=1証拠）
  figures/F-01.md ...   # 図解（Mermaid + データ表 + E-ID）
  report.md             # 最終レポート
  requests/needed_sources.md   # ユーザへの情報提供依頼
  checks/               # ゲート結果レポート
  improvements/         # 改善候補と採否ログ
```

---

## ステータス語彙（最重要）

不明を1語で潰さない。**原因が違えば次アクションが違う**ので、必ず区別する。

| 記号 | 意味 | 成立条件（これが無ければ使えない） |
|---|---|---|
| `[非公表]` | 情報源は特定できたが、そこに開示が無い | **確認した場所の証拠ID**（negative evidence）。例: 有報の該当章を確認し記載なし |
| `[未調査]` | 調査可能だが今回スコープ外／未実施 | 次アクション＋想定ソース＋優先度(H/M/L) |
| `[調査不可]` | アクセス障壁が確認済み | 障壁コード（PAYWALL/LOGIN/NOTFOUND/FETCH_FAIL/LANG/PRICED_DB）＋依頼ID |
| `[推定]` | 事実ではなく推論 | 根拠E-ID群＋推定ロジック＋確信度(高/中/低)＋反証条件 |
| `[未特定]` | 探索を尽くしたが確認できなかった | 必要証拠リストの項目ID(N-xx)＋探索ログの飽和条件（直近3件が経路もクエリも異なり新規0件） |

「不明」「情報が見つかりませんでした」だけの記述は `lint_status` がFAILにする。
判定フローと具体例は `references/status.md`。

---

## 証拠台帳のルール

1行1証拠のJSONL。必須フィールドとスキーマは `assets/ledger.schema.json`、運用規則は `references/evidence.md`。

```json
{"eid":"E-0007","source_id":"S-002","claim_type":"fact",
 "quote":"当連結会計年度の売上高は1,234,567百万円となりました",
 "char_start":18422,"char_end":18456,
 "fact":"25年3月期 売上高 1,234,567百万円",
 "value":{"number":1234567,"unit":"百万円","period":"25年3月期"},
 "basis_date":"2025-05-14","confidence":"高","tags":["財務.実績"]}
```

**引用の扱い**: `quote` は検証用のローカル作業データであり、原文完全一致でなければならない。
一方 **レポート本文では原則パラフレーズ**し、直接引用は1ソースにつき短い1箇所まで。
台帳の長文引用をレポートに貼り付けない（検証性は台帳側で担保されている）。

**ソース階層**（同一論点で矛盾したら上位を採る。矛盾は消さず両論併記＋採用理由を書く）:
T1 法定開示（有報/短信/10-K/10-Q/8-K/適時開示/目論見書）＞ T2 発行体一次資料（決算説明資料/書き起こし/IRサイト）
＞ T3 公的統計・規制当局・業界団体統計 ＞ T4 市場データ（株価・時価総額・株価指標。提供元は案件ごとに記録し、
取得手段は問わない）＞ T5 主要メディア・アナリスト ＞ T6 その他（ブログ/SNS/掲示板＝原則不採用、採用時は`confidence:低`固定）

**業績はT1〜T2から取る。** 市場データサイトの業績数値は二次加工されているため、業績の根拠には使わない。
T4は株価と株価指標に限る。T3は市場規模・シェアを発行体の自称に依存させないために必ず残す。

---

## スクリプト（判定はここでしか行わない）

すべて `python3 scripts/<name>.py <case_dir>` で実行。stdlibのみで動く（PDF抽出のみ任意依存）。

| スクリプト | 役割 | 使うタイミング |
|---|---|---|
| `init_case.py` | 案件ディレクトリ雛形＋scope.md生成 | P0 |
| `capture_source.py` | 保存済み原本→テキスト抽出、SHA256記録、未取得ソースの依頼草案生成 | P1終盤 |
| `build_ledger.py` | 台帳の整形・E-ID採番・オフセット自動補完 | P2 |
| `check_a_source_ledger.py` | **Wチェック①** ソース⇔台帳（引用の完全一致・ハッシュ・双方向網羅） | GA |
| `lint_status.py` | ステータス語彙と成立条件の検査 | GB |
| `check_b_ledger_report.py` | **Wチェック②** 台帳⇔レポート（E-ID解決・数値照合・無出典文検出・図表E-ID・取りこぼし） | GB |
| `run_gate.py` | 上記を一括実行し、ゲートレポートと**実行証跡**を生成 | 各ゲート |
| `extract_claims.py` | 主張⇔引用の突き合わせ表を生成（意味照合は人/別セッション） | GC |
| `verify_gate.py` | ゲートが実行され、その後改変されていないかを独立に確認 | 納品時 |
| `check_c_claims_review.py` | GCの判定が埋まり、不支持・部分的が残っていないかを確認 | GC完了時 |
| `check_l_search_log.py` | 探索ログの経路・クエリ多様性を検査（V37/V38相当） | P1随時 |
| `check_a_source_ledger.py`(A23〜A26) | Critical証拠の原典再アクセスを検証（収集直後） | P1直後 |
| `export_wcheck.py` | C-1/C-2/C-3相当のWチェック結果を証拠ごとに集約し、Excel出力（任意） | 納品前 |
| `improve_gate.py` | 改善候補の汎用性ゲート判定 | P5 |

**すべてのスクリプトは外部通信をしない。** ネットワークにも外部APIにも接続しないため、
実行によって課金が発生することはない（`tests/run_tests.py` が静的に検査する）。

**重要**: 取得（fetch）はスクリプトの責務ではない。Claudeのweb_fetch／ユーザ提供でraw/に置き、
スクリプトは「置かれた原文と書いた内容の一致」だけを機械判定する。
これにより「取得したつもり」の混入経路を構造的に断つ。

実行例:
```bash
python3 scripts/capture_source.py research/case1
python3 scripts/run_gate.py research/case1 --gate A
python3 scripts/run_gate.py research/case1 --gate B
python3 scripts/extract_claims.py research/case1   # → 表を新しいチャットに貼って意味照合
python3 scripts/run_gate.py research/case1 --gate C  # 判定が埋まったかを確認
python3 scripts/verify_gate.py research/case1 --gate B
```

**ゲートPASS後にレポートを書き換えない。** 書き換えると証跡のハッシュと合わなくなり、
`verify_gate.py` が検出する。修正したらゲートを再実行する。

---

## 図解（必須）

調査には図解を含める。図は装飾ではなく**数字の整合を目で検出する装置**として使う。
最小セット（対象が企業の場合）:

- **F-01 収益分解ツリー**: 売上→セグメント→ドライバ（数量×単価等）
- **F-02 バリューチェーン内位置と競合マップ**
- **F-03 業績・指標時系列**（最低5期、開示ベース）
- **F-04 仮説→ドライバ→検証指標（KPI）ツリー**：各仮説に崩壊条件を明記
- **F-05 リスク／感応度**（主要変数±で利益・株価指標がどう動くか。数値は`[推定]`扱い）

各図は `figures/F-xx.md` に **Mermaid（またはSVG）＋データ表＋各行のE-ID** を書く。
E-ID無しの数値が図に入っていると `check_b` がFAILにする。形式は `references/diagrams.md`。

---

### 対象企業の位置づけを見る（P3）

**このハーネスは個別企業の深掘り専用。** 候補を広く探して絞り込む
（ロングリスト生成・スクリーニング）用途には作られていない。
対象はユーザが指定する前提で、証拠台帳もゲートも図解も1社1案件を単位にしている。

その上で、**対象企業が業界のどの層にいて、その層が迂回可能かを見る**。
「良い会社か」ではなく「顧客がこの会社を迂回できるか」を問うと、
競争優位が構造として説明できる。判定の問いは `references/questions.md`（Q-A13〜A18）。

対象企業の分類を1つ選び、レポートに明記する:
**希少層を支配 / 希少層に供給 / トレンドの恩恵を受ける / 物語だけ**。
「この会社は何を制約しているのか」を一文で言えないなら、それは恩恵側にいるということ。

**この判断にスコアや点数を使わない。** 主観的な採点の合計は、根拠に遡れない数字が
「精密に見える結論」として独り歩きするため。
`[推定|根拠:E-xxxx|確信度:中|反証:～]` の形で、判断と根拠を分離して書く。

## ユーザへの情報提供依頼

アクセスできないPDF・有料DB・ログイン必須ページ・取得失敗が出たら、黙って諦めず**必ず依頼する**。
依頼は `requests/needed_sources.md` に蓄積し、レポートの該当箇所は `[調査不可]` + 依頼IDで紐づける。

依頼1件のフォーマット（テンプレは `references/ask-user.md`）:
```
### R-003 [優先度:H]
- 欲しいもの: ◯◯社 2025年3月期 有価証券報告書 セグメント情報（PDF or テキスト）
- なぜ必要: 仮説H2「データセンター向け比率が利益率を規定」の検証に不可欠
- 無い場合の影響: 収益分解F-01が推定のみとなり、結論の確信度が「中→低」に低下
- 代替案: 決算説明資料p.12でも可／EDINET直リンクでも可
- 提供方法: PDFをこのチャットに添付、またはテキスト貼り付け
```
**依頼は一度にまとめて出す**（小出しは往復コストが高い）。ユーザ提供後は必ず `raw/` に保存し、
`sources.json` に `provided_by:"user"` を記録してから引用する。

---

## レポート出力

構成テンプレは `assets/report_skeleton.md`。以下は崩さない:

- 冒頭に **調査サマリ・確信度・未解決事項（未調査/調査不可の一覧）** を置く
- 全事実記述に `[E-xxxx]`、推論には `[推定|根拠:E-0003,E-0011|確信度:中|反証条件:～]`
- 計算値には `[計算:E-0003,E-0011|式:(A-B)/B]`（`check_b`が再計算して照合）
- 最後に **投資判断への含意** と **崩れたら見直す条件（トリガー）**

外部システムへ取り込む形式で出力を求められた場合は、**その時点の現行仕様をユーザに確認してから**
形式を決める。Skillに固定形式を持たせない（仕様変更を検知できず、古い形式で出し続けるため）。

**投資判断について**: 本ハーネスは事実整理と仮説検証の道具であり、推奨（買え/売れ）を出す場所ではない。
含意は「この事実がどの仮説をどちらに動かすか」までに留め、最終判断はユーザに残す。

---

## 自己改善（P5）

調査後の会話には、ハーネスの欠陥情報が含まれる（「その数字どこから？」「この観点が抜けている」など）。
これを拾って改善するが、**その案件に最適化された改善は次の案件で害になる**ため、必ずゲートを通す。

1. 会話から改善候補を抽出 → `improvements/inbox.jsonl`（テンプレ `assets/improvement_candidate.template.json`）
2. `python3 scripts/improve_gate.py <case_dir>` で汎用性ゲート判定
   - **固有名詞テスト**: 特定銘柄・業種・時期に依存する記述はREJECT（一般化して再提出）
   - **反例テスト**: 別市場・別業種の2案件以上で有効と示せるか
   - **根本原因テスト**: 症状でなく失敗モードに効くか
   - **重複・矛盾テスト**: 既存ルールと衝突しないか
   - **コストテスト**: SKILL.md行数予算とステップ増加に見合うか
3. 判定 `PROMOTE` のみ SKILL.md／references に反映。**採否理由と却下も** `improvements/decision_log.md` に残す
4. 昇格条件: 原則**別案件で2回以上再発**。ただし *誤情報がレポートに載った* 種類の欠陥は**即時昇格**
5. 四半期ごとに未発火ルールを棚卸しして削る（肥大は遵守率を下げる）

詳細は `references/self-improve.md`。

---

## 自己テスト

チェック機構を変更したら必ず走らせる。全チェックコードについて
「発火すべきケース」と「発火してはいけないケース」を対で検証する（64件）。
外部通信コードが混入していないことも静的に検査する。

```bash
python3 tests/run_tests.py        # -v で全結果を表示
```

**片方向だけのテストをしない。** 検出できても誤検出だらけ、あるいはその逆の状態を見逃す。

## 参照ファイル

- `references/workflow.md` — フェーズ別の具体手順、日本株/米国株の一次情報源リスト
- `references/questions.md` — 問いの棚（共通コア＋テーマアドオン）。P0で仮説を立てた後に開く
- `references/evidence.md` — 台帳の書き方、ソース階層、矛盾の扱い
- `references/status.md` — 4ステータスの判定フローと誤用例
- `references/checker.md` — Wチェックの検査項目・FAIL/WARN基準・例外申請
- `references/diagrams.md` — 図の型とデータ表フォーマット
- `references/ask-user.md` — 情報提供依頼のトリガーとテンプレ
- `references/independent-review.md` — 独立レビュー（GC）。台帳とレポートを同じClaudeが書いている以上、
  意味の照合だけは書いた者以外が行う必要がある
- `references/self-improve.md` — 改善候補の抽出と汎用性ゲート
